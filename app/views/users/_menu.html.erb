<%= render_list class: "nav nav-tabs" do |li|
  li << link_to(t("users.menu.profile"), "#")
  li << link_to(t("users.menu.topics"), "#")
  li << link_to(t("users.menu.replies"), "#")
  li << link_to(t("users.menu.favorites"), "#")
  li << link_to(t("users.menu.notes"), "#")
  li << link_to(t("users.menu.following"), "#")
  li << link_to(t("users.menu.followers"), "#")
end %>
