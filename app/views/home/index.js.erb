//:remote=>true ajax 刷新数据
$('#articles').html('<%= escape_javascript render partial: 'articles/article', collection: @articles %>');
$('#paginator').html('<%= escape_javascript(paginate(@articles, :remote => true,:theme => 'bootstrap3').to_s) %>');